# minimal-mcp-server
This repo is a simple guide that walks through the simplest possible setup for a working MCP (Model Context Protocol) server that integrates with Cursor IDE. It requires no external packages, no SDKs, and no GitHub dependencies.
